package python.__modules__;

/**
 * Modules is a directory search for the index to any module.
 * <p>
 * Most modules will use the generic wrappers for all types. But in some cases a
 * module like numpy may require additional wrappers to improve functionality.
 * <p>
 * In those cases the Java package would create a new index here.
 */
